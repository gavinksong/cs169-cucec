<%= form_for(@micropost) do |f| %>
  <!--<%= render 'shared/error_messages', object: f.object %>-->
  <div class="field">
    <%= f.text_area :content, placeholder: "Add new material..." %>
  </div>
  <%= f.submit "Upload", class: "btn btn-primary", disabled: true %>
<% end %>